<Window x:Class="StudyAndOrder.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:StudyAndOrder.Wpf.Views"
        xmlns:vm="clr-namespace:StudyAndOrder.Wpf.ViewModels"
        Title="MainWindow" Height="600" Width="1000">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:OrdersViewModel}">
            <views:OrdersScreen/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:StudyInformationViewModel}">
            <views:StudyInformationScreen/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:OrderDetailsViewModel}">
            <views:OrderDetailsScreen/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <ContentControl Content="{Binding CurrentViewModel}" />
    </Grid>
</Window>